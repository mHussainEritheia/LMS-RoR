<% stripe_secret = ENV["stripe_secret"] %>
var stripe = Stripe("#{stripe_secret}");

stripe.redirectToCheckout({
    sessionId: '<%= @session.id%>'
  }).then(function (result) {
      console.log(result.error.message);
});